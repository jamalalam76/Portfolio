{
  "name": "portfolio-springboot",
  "version": "1.0.0",
  "description": "Personal Portfolio with AI Assistant (NVIDIA NIM)",
  "scripts": {
    "build": "chmod +x ./mvnw && ./mvnw clean package -DskipTests",
    "start": "java -jar target/portfolio-1.0.0.jar"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
